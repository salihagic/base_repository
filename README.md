# Storage repository
Abstraction for managing CRUD operations in flutter projects(communication with api).
You can also find this package on pub as [base_repository](https://pub.dev/packages/base_repository) 
